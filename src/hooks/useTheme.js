// import { useEffect } from "react";
// import { useSelector } from "react-redux";

const useTheme = () => {
  // This hook is no longer needed as its functionality has been moved to DefaultLayout.jsx
  // It is kept here as a placeholder to avoid import errors in older code that might still reference it.
};

export default useTheme;
